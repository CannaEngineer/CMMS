{
  "version": 2,
  "name": "cmms-app",
  "buildCommand": "cd backend && npm run vercel-build && cd ../frontend && npm run build && cd .. && cp backend/dist/src/index.js api/main.js && cp -r backend/dist/* api/ && cp backend/package.json api/",
  "outputDirectory": "frontend/dist",
  "installCommand": "cd backend && npm install && cd ../frontend && npm install && cd ../api && npm install",
  "functions": {
    "api/index.js": {
      "maxDuration": 30
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.js"
    },
    {
      "src": "/(.*)",
      "dest": "/frontend/dist/$1"
    }
  ]
}