# Database Configuration
# IMPORTANT: For Vercel deployment, set DATABASE_URL to ${POSTGRES_PRISMA_URL}
# This ensures you're using the pooled connection, not Data Proxy
DATABASE_URL="file:./dev.db"

# Optional: Direct connection for migrations (not pooled)
# For Vercel: Set to ${POSTGRES_URL} if you need direct connection for migrations
# DIRECT_DATABASE_URL=

# Frontend URL (for generating share links)
# In development: http://localhost:5173
# In production: https://your-domain.vercel.app
FRONTEND_URL=http://localhost:5173

# IP Hash Salt (change in production)
IP_HASH_SALT=change-this-in-production

# JWT Secret (change in production)
JWT_SECRET=your-secret-key

# Port
PORT=5000

# Email Configuration (MXroute SMTP)
SMTP_HOST=heracles.mxrouting.net
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=cmms@elevatedcompliance.tech
SMTP_PASS=Play101!@#$
SMTP_FROM=cmms@elevatedcompliance.tech
SMTP_FROM_NAME=Elevated Compliance CMMS

# Email Features
EMAIL_ENABLED=true
EMAIL_QUEUE_ENABLED=false