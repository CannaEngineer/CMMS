# ========================================
# PRODUCTION ENVIRONMENT VARIABLES
# Copy these to your Vercel dashboard
# ========================================

# Database Configuration - TURSO (Recommended)
DATABASE_URL="libsql://cmms-db-cannaengineer.aws-us-east-2.turso.io"
LIBSQL_URL="libsql://cmms-db-cannaengineer.aws-us-east-2.turso.io"
LIBSQL_AUTH_TOKEN="eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJpYXQiOjE3NTY0ODE1MjEsImlkIjoiM2RhNDliOGYtNDQ1MC00MWI1LTg2MDEtODY5Zjk3YjJkZTRjIiwicmlkIjoiNzdlOGUzMWYtMmFmNC00MDRjLWFmZWMtMjE5YzFhNWQ2NjI0In0.8C1Ctp_WitPUgWzOQLGUuulCUPZT-75DHY6ifJvPuPN-_r8CT8j95vnxEEZdyqchebRnE1X3pqaRgpB5-QgrBA"

# Alternative - Local SQLite (fallback if Turso has issues)
# DATABASE_URL="file:./prisma/dev.db"

# Frontend Configuration
FRONTEND_URL="https://your-frontend-domain.vercel.app"

# Security Configuration (CHANGE THESE!)
IP_HASH_SALT="your-unique-production-salt-here"
JWT_SECRET="your-secure-jwt-secret-minimum-32-characters"

# Application Configuration
PORT="5000"
NODE_ENV="production"

# Email Configuration
SMTP_HOST="heracles.mxrouting.net"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="cmms@elevatedcompliance.tech"
SMTP_PASS="your-actual-smtp-password"
SMTP_FROM="cmms@elevatedcompliance.tech"
SMTP_FROM_NAME="Elevated Compliance CMMS"
EMAIL_ENABLED="true"
EMAIL_QUEUE_ENABLED="false"

# Vercel Blob Storage
BLOB_READ_WRITE_TOKEN="your-production-vercel-blob-token"

# ========================================
# IMPORTANT SECURITY NOTES:
# ========================================
# 1. Generate a new JWT_SECRET: openssl rand -base64 32
# 2. Generate a new IP_HASH_SALT: openssl rand -hex 16
# 3. Get your production SMTP password from your email provider
# 4. Get your production Vercel Blob token from Vercel dashboard
# 5. Replace FRONTEND_URL with your actual frontend domain
# ========================================